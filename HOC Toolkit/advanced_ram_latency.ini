;show_widget=true

[Advanced Latency]

[*Read Latency 0?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 92 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values read_latency_1333 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Read Latency 1?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 96 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values read_latency_1600 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Read Latency 2?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 100 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values read_latency_1866 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Read Latency 3?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 104 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values read_latency_2133 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[gap]
;mode=table
;background=false
;gap=20


[*Write Latency 0?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 108 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values write_latency_1333 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Write Latency 1?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 112 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values write_latency_1600 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Write Latency 2?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 116 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values write_latency_1866 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back

[*Write Latency 3?RAMmariko]
;system=mariko
;mode=option
;mini=true
;selection_mini=true
json_file_source './json/latency_freqs.json' label
hex-by-custom-offset /atmosphere/kips/hoc.kip CUST 120 {json_file_source(*,hex)}
set-ini-val './config.ini' 'globals' 'crc32' '{crc32(/atmosphere/kips/hoc.kip)}'
mv ./notifications/hoc-reboot_required_once.notify /config/ultrahand/notifications/
set-footer '{json_file_source(*,name)}'
; --- sync changed value into hoc-clk config.ini when flag present (no kip reread) ---
try:
path_exists ./flags/UPDATE_HOC_CONFIG.flag
set-ini-val /config/horizon-oc/config.ini values write_latency_2133 {hex_to_decimal({hex_to_rhex({json_file_source(*,hex)})})}
set-ini-val /config/horizon-oc/config.ini values is_first_load 1
back
try:
back
